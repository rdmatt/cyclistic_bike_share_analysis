import glob
... import numpy as np
... import pandas as pd
...
... # 1. Carregar os dados unificados
... caminho = r"C:\Users\USUARIO\PASTA\*.csv"  # Ajuste o caminho
... arquivos = glob.glob(caminho)
... df_completo = pd.concat(
...     (pd.read_csv(f) for f in arquivos), ignore_index=True
... )
...
... print("--- PROCESSAMENTO E LIMPEZA DE DADOS ---")
...
... # 2. Converter colunas de data/hora para o formato datetime
... df_completo['started_at'] = pd.to_datetime(df_completo['started_at'])
... df_completo['ended_at'] = pd.to_datetime(df_completo['ended_at'])
...
... # 3. Criar a coluna ride_length (duração da viagem em minutos)
... df_completo['ride_length'] = (
...     df_completo['ended_at'] - df_completo['started_at']
... ).dt.total_seconds() / 60
...
... # 4. Criar a coluna day_of_week (nome do dia da semana em português)
... dias_semana = {
...     'Sunday': 'Domingo',
...     'Monday': 'Segunda-feira',
...     'Tuesday': 'Terça-feira',
...     'Wednesday': 'Quarta-feira',
...     'Thursday': 'Quinta-feira',
...     'Friday': 'Sexta-feira',
...     'Saturday': 'Sábado',
... }
... df_completo['day_of_week'] = (
...     df_completo['started_at'].dt.day_name().map(dias_semana)
... )
...
... # 5. Criar coluna de mês/ano para analisar estacionalidade
... df_completo['month_year'] = df_completo['started_at'].dt.to_period('M')
...
... # 6. Filtrar dados discrepantes/inválidos (viagens <= 0 minutos ou testes de manutenção)
... df_limpo = df_completo[
...     (df_completo['ride_length'] > 0)
...     & (~df_completo['start_station_name'].str.contains('TEST', na=False))
... ].copy()
...
... linhas_removidas = len(df_completo) - len(df_limpo)
... print(f"Linhas removidas (erros/testes): {linhas_removidas:,}")
... print(f"Total de registros limpos: {len(df_limpo):,}\n")
...
... print("--- ANÁLISE EXPLORATÓRIA DOS DADOS ---")
...
... # A. Comparação Geral: Membros vs Casuais
... resumo_geral = (
...     df_limpo.groupby('member_casual')
...     .agg(
...         total_viagens=('ride_id', 'count'),
...         duracao_media_min=('ride_length', 'mean'),
...         duracao_mediana_min=('ride_length', 'median'),
...         duracao_max_min=('ride_length', 'max'),
...     )
...     .reset_index()
... )
...
... print("1. Resumo Geral por Tipo de Usuário:")
... print(resumo_geral.to_string(index=False))
... print("\n" + "=" * 50 + "\n")
...
... # B. Volume de Viagens e Duração Média por Dia da Semana
... ordem_dias = [
...     'Domingo',
...     'Segunda-feira',
...     'Terça-feira',
...     'Quarta-feira',
...     'Quinta-feira',
...     'Sexta-feira',
...     'Sábado',
... ]
...
... resumo_dias = (
...     df_limpo.groupby(['member_casual', 'day_of_week'])
...     .agg(
...         total_viagens=('ride_id', 'count'),
...         duracao_media_min=('ride_length', 'mean'),
...     )
...     .reset_index()
... )
...
... resumo_dias['day_of_week'] = pd.Categorical(
...     resumo_dias['day_of_week'], categories=ordem_dias, ordered=True
... )
... resumo_dias = resumo_dias.sort_values(['member_casual', 'day_of_week'])
...
... print("2. Desempenho por Dia da Semana:")
... print(resumo_dias.to_string(index=False))
...
... # 7. Salvar um arquivo resumo leve para usar no Tableau/Power BI
... resumo_dias.to_csv('resumo_membros_casuais.csv', index=False)
... print(
...     "\nArquivo 'resumo_membros_casuais.csv' exportado com sucesso para a criação de gráficos!"
... )
>>> # 1. Agrupamento para Gráfico de Dias da Semana (Volume e Duração Média)
... df_dias = (
...     df_limpo.groupby(['member_casual', 'day_of_week'])
...     .agg(
...         total_viagens=('ride_id', 'count'),
...         duracao_media=('ride_length', 'mean'),
...     )
...     .reset_index()
... )
... df_dias.to_csv('tableau_dias_semana.csv', index=False)
...
... # 2. Agrupamento para Gráfico de Meses/Estacionalidade
... df_limpo['mes'] = df_limpo['started_at'].dt.strftime('%Y-%m')
... df_meses = (
...     df_limpo.groupby(['member_casual', 'mes'])
...     .agg(
...         total_viagens=('ride_id', 'count'),
...         duracao_media=('ride_length', 'mean'),
...     )
...     .reset_index()
... )
... df_meses.to_csv('tableau_meses.csv', index=False)
...
... # 3. Agrupamento por Tipo de Bicicleta
... df_bikes = (
...     df_limpo.groupby(['member_casual', 'rideable_type'])
...     .agg(total_viagens=('ride_id', 'count'))
...     .reset_index()
... )
... df_bikes.to_csv('tableau_tipos_bike.csv', index=False)
